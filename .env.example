# Project-wide Environment Configuration
# Copy this file to .env and update the values

# Development Mode
NODE_ENV=development

# Database Configuration (same as server/.env)
MONGODB_URI=mongodb://localhost:27017/ecomstore

# JWT Configuration (same as server/.env)
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production

# Server Configuration
SERVER_PORT=5000
CLIENT_PORT=3000

# API Base URL
API_BASE_URL=http://localhost:5000/api

# For production deployment:
# NODE_ENV=production
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ecomstore
# API_BASE_URL=https://your-domain.com/api

# Optional: CORS settings
# CORS_ORIGIN=http://localhost:3000

# Optional: File upload limits
# MAX_FILE_SIZE=5000000
# UPLOAD_PATH=./uploads
